apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-site-chart.fullname" . }}
  labels:
    {{- include "bank-site-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-site-chart.selectorLabels" . | nindent 4 }}
