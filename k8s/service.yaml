apiVersion: v1
kind: Service
metadata:
  name: bank-site-service
spec:
  selector:
    app: bank-site
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
